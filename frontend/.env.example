# REQUIRED when API runs on RunPod (do not use trailing slash).
# Get URL from RunPod → Connect → HTTP services → Port 8000
# If frontend/.env already exists, edit it — do NOT blind-copy this file over it.
VITE_API_BASE=https://YOUR-POD-ID-8000.proxy.runpod.net
